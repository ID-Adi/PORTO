# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
out/
dist/
build/
.turbo/

# TypeScript
*.tsbuildinfo

# Test / coverage
coverage/

# Environment files (anywhere in repo)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# Vercel
.vercel/

# Claude Code worktrees (lokal, jangan ikut ke-track sebagai embedded repo)
.claude/worktrees/

# Kiro CLI workspace settings (MCP config, agents, skills cache, dsb)
.kiro/

# N8N workflow exports — sering memuat access token / credential live.
# File v2 (yang sanitized via $env.*) di-allow lewat exception di bawah.
GEMINI_3.1_GENERATE.json
*.workflow.secret.json

# Allow examples
!.env.example

# Playwright MCP artefak sesi (log, snapshot, screenshot) — transient.
.playwright-mcp/

# Codex CLI local config
.codex/

# Database backups (dihasilkan deploy.sh — tidak perlu di-track)
backups/
