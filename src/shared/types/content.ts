export type HeroContent = {
  badge: string;
  title: string;
  description: string;
  location: string;
  highlights: string[];
};

export type SummaryItem = {
  title: string;
  description: string;
};

export type CraftRule = {
  title: string;
  body: string;
};

