# GitHub Personal Access Token — needs read:user scope for contributions
GITHUB_TOKEN=
GITHUB_USERNAME=prasetya

# Backend (Hono + tRPC + Better Auth) base URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:4001

# Email of the single admin allowed into /admin (must match backend ADMIN_EMAIL)
NEXT_PUBLIC_ADMIN_EMAIL=admin@porto.dev
